Jira-like flask web app.
For first run (powershell) create and activate venv:
  python -m venv /path/to/new/virtual/environment
  .\venv\Scripts\activate
and run "first_run.bat" script:
  ./first_run.bat
